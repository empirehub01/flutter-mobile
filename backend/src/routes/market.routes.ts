import express, { Router } from 'express';

const router: Router = express.Router();

// TODO: Implement market data routes
// GET /prices/:symbol - Get current price
// GET /chart/:symbol - Get candlestick data
// GET /exchanges - List supported exchanges
// GET /trending - Get trending assets

export { router };
